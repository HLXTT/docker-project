events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        location /images/ {
            root /usr/share/nginx/html;
            autoindex on;
            expires 30d;
        }
    }
}
